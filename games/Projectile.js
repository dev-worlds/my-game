import AbstractGenericClass from "./AbstractGenericClass.js";

class Projectile extends AbstractGenericClass {
    constructor(context, config, direction) {
        super(context, config, direction)
    }
}

export default Projectile;